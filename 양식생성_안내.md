# 수행평가 양식 생성기 · 설치와 사용 안내 (v8)

3학년 「자료구조」 · 2026학년도 2학기

웹앱 하나로 **프롬프트 만들기 → 제미나이에 묻기 → 답을 붙여넣어 문서 만들기** 까지 끝냅니다.

| | |
|---|---|
| 코드 | `양식생성_코드.gs` |
| 만들어지는 것 | 수행평가 1·2 × 구글 문서 / 구글 슬라이드 |
| 파일 이름 | `3학년 3반 7번 김서연 - 수행평가 2 탐색 알고리즘 설계` |
| 저장 위치 | 학생 드라이브의 `자료구조 수행평가` 폴더 (없으면 자동 생성) |

> **내용은 채워지지 않습니다.** 항목 제목·안내·빈 표·`【 】` 자리만 만들어집니다.
> 프롬프트 안에 «틀의 내용을 채우지 마» 를 못박아 두었습니다.

---

## 학생이 밟는 순서

### 1단계 · 내 정보 넣기

수행평가 번호를 고르면 **그에 맞는 입력칸이 자동으로 바뀝니다.**

| | 입력칸 |
|---|---|
| 수행평가 1 | 주제 / 찾거나 순서를 매기고 싶은 것 |
| 수행평가 2 | 주제 / 자료 규모 / 정렬 여부 / 추가삭제 빈도 |

모르는 칸은 비워 둡니다. → **「반영해서 프롬프트 만들기」**

### 2단계 · 프롬프트 복사해서 제미나이에 묻기

**내 정보가 이미 채워진** 프롬프트가 나옵니다. 복사 버튼을 누르고 **「제미나이 열기」** 로 이동해 붙여넣습니다.

제미나이 답변 맨 아래에 이런 부분이 들어 있습니다.

```
=== 문서 틀 시작 ===
# 탐색 알고리즘 설계
## 1. 탐구 주제 명칭 및 자료의 특성 분석
> 무엇을 키로 삼아 무엇을 찾는지 한 문장으로 쓰세요.
▸ 탐구 주제 명칭 및 선정 이유
【                    】
| 단계 | 비교한 값 | 남은 탐색 범위 | 판단 |
| --- | --- | --- | --- |
|  |  |  |  |
=== 문서 틀 끝 ===
```

### 3단계 · 붙여넣고 모양 골라 만들기

답변을 붙여넣습니다. **답변 전체를 붙여넣어도 됩니다** — 웹앱이 `=== 문서 틀 시작 ===` 부터 `=== 문서 틀 끝 ===` 까지만 알아서 골라냅니다.

고를 수 있는 것:

| 항목 | 선택지 |
|---|---|
| 형태 | 구글 문서 / 구글 슬라이드 |
| 색 | 청록 · 남색 · 자주 · 흑백(인쇄용) |
| 글꼴 | 본고딕 · 나눔고딕 · 맑은 고딕 · 바탕 |
| 글자 크기 | 보통 · 크게 |
| 표지 | 넣기 / 안 넣기 |
| 목차 | 넣기 / 안 넣기 |
| 항목별 안내 문구 | 넣기 / 안 넣기 |
| **제미나이가 준 참고 재료** | **문서 뒤에 함께 넣기 / 안 넣기** |

**붙여넣기 칸을 비워 두고 만들면** 코드에 정의된 **기본 양식**으로 만들어집니다.
제미나이를 쓰지 않는 학생도 그대로 쓸 수 있습니다.

### 문서에 무엇이 들어가나

제미나이는 항목마다 **완성된 초안**과 **확인·수정할 곳**을 써 줍니다.
웹앱은 그 아래에 **「내가 고쳐 쓴 글」** 빈자리를 자동으로 넣습니다.



**채점은 「내가 고쳐 쓴 글」만 봅니다.** 문서 맨 앞에도 이 안내가 자동으로 붙습니다.
초안을 그대로 두고 내면 평가 규정에 따라 해당 평가요소 최하점입니다.

수행평가 2의 **실행 결과 항목(③④)** 은 제미나이가 숫자를 지어내지 않고
 로 비워 둡니다. 학생이 코드를 돌려 채웁니다.


붙여넣은 답변은 두 부분으로 나뉘어 들어갑니다.

| 부분 | 어디로 |
|---|---|
| `=== 문서 틀 시작/끝 ===` **사이** | **본문** — 학생이 채울 제출물 |
| 그 **바깥의 나머지 전부** (개념 정리 · 실습 데이터 · 확인할 것 …) | **뒤쪽 「참고 재료」** |

「참고 재료」 앞에는 가로선과 함께 이런 문구가 자동으로 붙습니다.

> 이 아래는 제출물이 아닙니다. 여기 있는 내용을 그대로 옮겨 적으면 최하점입니다.
> 읽고 이해한 다음 위쪽 【 】 자리에 내 문장으로 쓰고, 제출 전에 이 부분을 지우세요.

**표시를 못 찾아도 내용이 버려지지 않습니다.** 기본 양식으로 만들고,
붙여넣은 글 전체를 「참고 재료」에 넣은 뒤 경고를 함께 보여 줍니다.

제미나이가 표시를 `**굵게**` 하거나 ``` 로 감싸도 알아봅니다.
``` 로 감싼 코드(실습 데이터 등)는 **고정폭 글꼴로 원문 그대로** 옮겨집니다.

---

## 기호와 문서 모양의 대응

붙여넣은 글을 이 규칙으로 문서로 옮깁니다.

| 붙여넣은 줄 | 문서에서 |
|---|---|
| `# 글자` | 제목 (TITLE) |
| `## 글자` | 항목 제목 (제목 1) · 슬라이드에서는 **장표 한 장** |
| `### 글자` | 작은 제목 (제목 2) |
| `▸ 글자` | 질문 (제목 3) |
| `> 글자` | 회색 기울임 안내 — 「안내 문구 안 넣기」를 고르면 빠짐 |
| `【 】` | 학생이 쓸 회색 빈 자리 |
| `- 글자` / `· 글자` | 글머리 기호 목록 |
| `\| 가 \| 나 \|` | 표. 둘째 줄 `\| --- \|` 는 구분선이라 표에 안 들어감 |
| `---` | 가로선 |

---

## 설치 (한 번만, 5분)

1. [script.google.com](https://script.google.com) → **새 프로젝트**
2. 이름을 `자료구조 수행평가 양식 생성기` 로 변경
3. `Code.gs` 내용을 모두 지우고 `양식생성_코드.gs` 를 통째로 붙여넣기
4. 위쪽 두 줄 확인

   ```javascript
   var 폴더이름 = '자료구조 수행평가';
   var 교사이메일 = '';
   ```

   `교사이메일` 에 주소를 적으면 학생이 만든 문서가 **자동으로 공유**됩니다.
   학생이 링크를 직접 제출하는 방식이면 비워 두세요.

5. 저장

## 먼저 혼자 시험해 보기

함수 목록에서 **`테스트만들기`** 를 골라 **실행**.

- 처음에는 권한을 물어봅니다 → **검토 → 계정 선택 → 고급 → 이동 → 허용**
- 실행 기록에 프롬프트 앞부분이 찍히고, 드라이브에 **문서 2개**가 생기면 성공입니다
  (하나는 기본 양식, 하나는 «붙여넣은 틀» 을 흉내 낸 남색·나눔고딕·목차 포함본)
- 확인 후 지우세요

## 배포 (학생용 주소 만들기)

1. **배포 → 새 배포** → 톱니바퀴(⚙) → **웹 앱**
2. 아래처럼 맞춥니다.

   | 항목 | 설정 |
   |---|---|
   | 실행 대상 | **웹 앱에 액세스하는 사용자** |
   | 액세스 권한 | 학교 도메인 소속 사용자 |

3. **배포** → 나온 **웹 앱 URL** 복사

> **「실행 대상」을 «나»로 두면 안 됩니다.**
> 학생 문서가 전부 선생님 드라이브에 쌓입니다.

## 안내 페이지에 연결하기

`page.html` 아래쪽 스크립트에서 주소를 넣습니다.

```javascript
var 양식생성주소 = 'https://script.google.com/macros/s/.../exec';
```

넣으면 수행평가 1·2 탭에 **「양식 문서 바로 만들기」** 버튼이 나타납니다.
고친 뒤 `sync.ps1` 을 실행하면 배포본에 반영됩니다.

```bash
powershell -ExecutionPolicy Bypass -File sync.ps1
```

---

## 양식을 바꾸고 싶을 때

`양식생성_코드.gs` 위쪽 `var 양식 = { ... }` 하나만 고치면
**기본 양식 · 프롬프트 · 슬라이드가 모두 함께** 바뀝니다.

```javascript
{
  번호: '3', 제목: '코드 실행 및 시뮬레이션', 배점: '평가요소 ③ · 25점',
  안내: '실행 결과 없이 표만 있으면 상위 수준으로 못 갑니다.',
  칸: ['코드 A 실행 결과 — 출력 화면을 그대로 붙여넣기'],
  표: { 이름: '탐색 과정 표',
        머리글: ['단계', '비교한 값', '남은 탐색 범위', '판단'],
        줄수: 6 }
}
```

- `입력` — 1단계에서 학생이 채울 칸. `고르기` 를 주면 드롭다운이 됩니다
- `칸` — 질문 목록. 질문마다 `【 】` 빈 자리가 붙습니다
- `표.줄수` — 빈 줄 개수 / `표.줄` — 미리 채워 둘 값 (수행평가 2의 `log2(n)` 열)

색을 바꾸려면 `var 테마 = { ... }` 에 항목을 추가하면 3단계 화면에 자동으로 나타납니다.

고친 뒤에는 **배포 → 배포 관리 → 연필(✏) → 버전 «새 버전» → 배포**.
주소는 그대로입니다.

---

## 잘 안 될 때

| 증상 | 해결 |
|---|---|
| «권한이 없습니다» | 배포의 **액세스 권한**을 학교 도메인으로 |
| 문서가 선생님 드라이브에 쌓임 | **실행 대상**을 «웹 앱에 액세스하는 사용자» 로 바꿔 **새 버전** 배포 |
| 고쳤는데 그대로임 | **새 버전**으로 배포했는지 확인 |
| 버튼이 안 보임 | `page.html` 의 `양식생성주소` 가 비어 있음 |
| «프롬프트로 보입니다» 라고 나옴 | 2단계 프롬프트를 제미나이에 넣지 않고 그대로 3단계에 붙여넣은 것. 제미나이 답변을 붙여넣으세요 |
| «틀 표시를 찾지 못했습니다» 경고 | 제미나이가 `=== 문서 틀 시작 ===` 을 빼먹은 것. 기본 양식으로는 만들어져 있으니, 제미나이에 «문서 틀 부분을 다시 줘» 라고 하면 됩니다 |
| 복사 버튼이 안 먹음 | 브라우저가 막은 경우. 칸을 눌러 **Ctrl+A → Ctrl+C** 로 직접 복사 |
| 슬라이드에 표가 없음 | 슬라이드는 표를 자동 생성하지 않습니다. 안내 줄만 들어가고 학생이 직접 넣습니다 |

---

## 더 간단한 대안 — 템플릿 사본 링크

Apps Script 없이도 됩니다. 문서 이름을 학생이 직접 바꿔야 한다는 점만 다릅니다.

1. 양식 문서를 하나 만들어 둡니다
2. 공유를 **링크가 있는 학교 사용자 · 뷰어** 로 설정
3. 주소 끝의 `/edit` 을 **`/copy`** 로 바꿔 학생에게 줍니다

```
https://docs.google.com/document/d/문서ID/copy
```

권한 허용 절차가 없어 걸림돌이 가장 적습니다.
