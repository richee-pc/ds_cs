# 수행평가 양식 생성기 · 설치와 사용 안내

3학년 「자료구조」 · 2026학년도 2학기

학생이 주소를 열고 **반·번호·이름만 넣으면** 자기 구글 드라이브에
**빈 양식** 문서(또는 슬라이드)가 만들어집니다.

| | |
|---|---|
| 코드 | `양식생성_코드.gs` |
| 만들어지는 것 | 수행평가 1·2 × 구글 문서 / 구글 슬라이드 |
| 파일 이름 | `3학년 3반 7번 김서연 - 수행평가 2 탐색 알고리즘 설계` |
| 저장 위치 | 학생 드라이브의 `자료구조 수행평가` 폴더 (없으면 자동 생성) |

> **내용은 하나도 채워지지 않습니다.** 항목 제목과 안내, 빈 표, 그리고
> `【      】` 자리만 만들어집니다. 채워서 주면 평가 규정 위반이라 그렇게 설계했습니다.

---

## 1. 설치 (한 번만, 5분)

1. [script.google.com](https://script.google.com) 접속 → **새 프로젝트**
2. 프로젝트 이름을 `자료구조 수행평가 양식 생성기` 로 바꿉니다.
3. 왼쪽 `Code.gs` 의 내용을 **모두 지우고**, `양식생성_코드.gs` 의 내용을 **통째로 붙여넣습니다.**
4. 파일 위쪽 두 줄을 확인합니다.

   ```javascript
   var 폴더이름 = '자료구조 수행평가';
   var 교사이메일 = '';
   ```

   - `교사이메일` 에 선생님 주소를 적으면 학생이 만든 문서가 **자동으로 공유**됩니다.
   - 학생이 링크를 직접 제출하는 방식이면 **비워 두세요.**

5. 저장(💾)합니다.

## 2. 먼저 혼자 시험해 보기

왼쪽 함수 목록에서 **`테스트만들기`** 를 골라 **실행**을 누릅니다.

- 처음에는 구글이 권한을 물어봅니다 → **검토 → 계정 선택 → 고급 → 이동 → 허용**
- 내 드라이브 `자료구조 수행평가` 폴더에 **문서 2개, 슬라이드 1개**가 생기면 성공입니다.
- 확인 후 그 세 개는 지우세요.

## 3. 배포 (학생용 주소 만들기)

1. 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴(⚙) → **웹 앱**
3. 아래처럼 맞춥니다.

   | 항목 | 설정 |
   |---|---|
   | 설명 | `v1` |
   | 실행 대상 | **웹 앱에 액세스하는 사용자** ← **꼭 이걸로** |
   | 액세스 권한 | **조선대학교부속고등학교 소속 사용자 전체** (또는 학교 도메인) |

4. **배포** → 나오는 **웹 앱 URL** 을 복사합니다.

> **「실행 대상」을 «나»로 두면 안 됩니다.** 그러면 문서가 전부
> 선생님 드라이브에 만들어지고 곧 용량이 찹니다.
> **«웹 앱에 액세스하는 사용자»** 로 두어야 학생 드라이브에 학생 소유로 만들어집니다.

## 4. 안내 페이지에 주소 연결하기

`page.html` (= `수행평가_안내_비선형구조-알고리즘.html`) 아래쪽 스크립트에서
이 줄을 찾아 방금 복사한 주소를 넣습니다.

```javascript
var 양식생성주소 = '';
```

```javascript
var 양식생성주소 = 'https://script.google.com/a/macros/.../exec';
```

주소를 넣으면 **수행평가 1·2 탭의 프롬프트 칸 아래에 「양식 문서 바로 만들기」 버튼**이
나타납니다. 비워 두면 버튼은 보이지 않습니다.

고친 뒤 `sync.ps1` 을 실행하면 배포본에 반영됩니다.

```bash
powershell -ExecutionPolicy Bypass -File sync.ps1
```

---

## 학생이 하는 일

1. 안내 페이지의 **「양식 문서 바로 만들기」** 를 누릅니다.
2. 수행평가 번호 / 문서 or 슬라이드 / 반 / 번호 / 이름 을 넣고 **양식 만들기**.
3. 처음 한 번만 구글이 권한을 물어봅니다 → **학교 계정으로 허용**.
4. 새 탭으로 문서가 열립니다. `【 】` 자리를 자기 문장으로 채우면 됩니다.

---

## 양식 내용을 바꾸고 싶을 때

`양식생성_코드.gs` 위쪽의 `var 양식 = { ... }` 만 고치면 됩니다.
문서와 슬라이드가 **같은 정의**를 씁니다.

```javascript
{
  번호: '3', 제목: '코드 실행 및 시뮬레이션', 배점: '평가요소 ③ · 25점',
  안내: '실행 결과 없이 표만 있으면 상위 수준으로 못 갑니다.',
  칸: ['코드 A 실행 결과 — 출력 화면을 그대로 붙여넣기',
       '코드 B 실행 결과 — 출력 화면을 그대로 붙여넣기'],
  표: { 이름: '탐색 과정 표',
        머리글: ['단계', '비교한 값', '남은 탐색 범위', '판단'],
        줄수: 6 }
}
```

- `칸` — 항목 하나에 만들 질문들. 질문 아래마다 `【 】` 빈 자리가 생깁니다.
- `표.줄수` — 빈 줄 개수
- `표.줄` — 미리 채워 둘 값이 있을 때 (수행평가 2의 `log2(n)` 열이 이 방식입니다)

고친 뒤에는 **배포 → 배포 관리 → 연필(✏) → 버전 «새 버전» → 배포** 를 해야
학생 화면에 반영됩니다. 주소는 그대로입니다.

---

## 잘 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| 학생이 «권한이 없습니다» | 배포의 **액세스 권한**이 학교 도메인으로 되어 있는지 확인 |
| 문서가 선생님 드라이브에 쌓임 | **실행 대상**이 «나» 로 되어 있음 → «웹 앱에 액세스하는 사용자» 로 바꿔 **새 버전** 배포 |
| 고쳤는데 그대로임 | 배포 관리에서 **새 버전**으로 배포했는지 확인 |
| 버튼이 안 보임 | `page.html` 의 `양식생성주소` 가 비어 있음 |
| 슬라이드에 표가 없음 | 슬라이드는 표를 자동으로 만들지 않습니다. 안내 줄만 들어가고 학생이 직접 표를 넣습니다. |

---

## 더 간단한 대안 — 템플릿 사본 링크

Apps Script 없이도 됩니다. 다만 **문서 이름을 학생이 직접 바꿔야** 합니다.

1. 선생님이 양식 문서를 하나 만들어 둡니다.
2. 공유를 **링크가 있는 학교 사용자 · 뷰어** 로 설정합니다.
3. 주소 끝의 `/edit` 을 **`/copy`** 로 바꾼 링크를 학생에게 줍니다.

```
https://docs.google.com/document/d/문서ID/copy
```

학생이 누르면 «사본 만들기» 화면이 나오고, 누르면 자기 드라이브에 사본이 생깁니다.
권한 허용 절차가 없어 가장 걸림돌이 적습니다.
**반·번호·이름 자동 입력과 폴더 정리가 필요 없다면 이 방법으로 충분합니다.**
